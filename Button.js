import React from "react";
  
    // Create a functional component
    const Button = (props) => {

        return (<div className="app">;)<button onClick={props.callApi}>
         GET A JOKE
        </button>(;
         </div>);
    }
  
// Export Button Component
export default Button;